import java.util.Scanner;

public class Main {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		

	
 
		/*
		 * Leia 2 valores de ponto flutuante de dupla precisão A e B, que correspondem a
		 * 2 notas de um aluno. A seguir, calcule a média do aluno, sabendo que a nota A
		 * tem peso 3.5 e a nota B tem peso 7.5 (A soma dos pesos portanto é 11). Assuma
		 * que cada nota pode ir de 0 até 10.0, sempre com uma casa decimal.
		 * 
		 * Entrada O arquivo de entrada contém 2 valores com uma casa decimal cada um.
		 * 
		 * Saída Imprima a mensagem "MEDIA" e a média do aluno conforme exemplo abaixo,
		 * com 5 dígitos após o ponto decimal e com um espaço em branco antes e depois
		 * da igualdade. Utilize variáveis de dupla precisão (double) e como todos os
		 * problemas, não esqueça de imprimir o fim de linha após o resultado, caso
		 * contrário, você receberá "Presentation Error".
		 */

		/*
		 * Leia 3 valores, no caso, variáveis A, B e C, que são as três notas de um
		 * aluno. A seguir, calcule a média do aluno, sabendo que a nota A tem peso 2, a
		 * nota B tem peso 3 e a nota C tem peso 5. Considere que cada nota pode ir de 0
		 * até 10.0, sempre com uma casa decimal.
		 * 
		 * Entrada O arquivo de entrada contém 3 valores com uma casa decimal, de dupla
		 * precisão (double).
		 * 
		 * Saída Imprima a mensagem "MEDIA" e a média do aluno conforme exemplo abaixo,
		 * com 1 dígito após o ponto decimal e com um espaço em branco antes e depois da
		 * igualdade. Assim como todos os problemas, não esqueça de imprimir o fim de
		 * linha após o resultado, caso contrário, você receberá "Presentation Error".
		 */
//
//		double base = 0f, area = 0f, perimetro = 0f, diagonal = 0f;
//		double altura = 0f;
//
//		
//
//		base = sc.nextDouble();
//		altura = sc.nextDouble();
//
//		area = base * altura;
//		perimetro = (2 * base) + (2 * altura);
//		diagonal = Math.sqrt(Math.pow(base, 2) + Math.pow(altura, 2));
//
//		System.out.printf("area %.2f altura %.4f diagnal %.4f", area, perimetro, diagonal);
//
//		String nome_pessoa1;
//		String nome_pessoa2;
//		int idade_pessoa1;
//		int idade_pessoa2;
//		double diferenca_idade;
//
//		System.out.println(" digite o nome pessoa 1");
//		nome_pessoa1 = sc.next();
//		System.out.println(" digite o nome pessoa 2");
//		nome_pessoa2 = sc.next();
//		System.out.println(" digite o idade pessoa 1");
//		idade_pessoa1 = sc.nextInt();
//		System.out.println(" digite o idade pessoa 1");
//		idade_pessoa2 = sc.nextInt();
//
//		diferenca_idade = (double) (idade_pessoa1 + idade_pessoa2) / 2;
//
//		System.out.printf("%s %d %n", nome_pessoa1, idade_pessoa1);
//		System.out.printf("%s %d %n", nome_pessoa2, idade_pessoa2);
//		System.out.printf("A idade Média entre %s e %s é de %.1f", nome_pessoa1, nome_pessoa2, diferenca_idade);

		

		

		/*
		 * Faça um programa que calcule e mostre o volume de uma esfera sendo fornecido
		 * o valor de seu raio (R). A fórmula para calcular o volume é: (4/3) * pi * R3.
		 * Considere (atribua) para pi o valor 3.14159.
		 * 
		 * Dica: Ao utilizar a fórmula, procure usar (4/3.0) ou (4.0/3), pois algumas
		 * linguagens (dentre elas o C++), assumem que o resultado da divisão entre dois
		 * inteiros é outro inteiro.
		 * 
		 * Entrada O arquivo de entrada contém um valor de ponto flutuante (dupla
		 * precisão), correspondente ao raio da esfera.
		 * 
		 * Saída A saída deverá ser uma mensagem "VOLUME" conforme o exemplo fornecido
		 * abaixo, com um espaço antes e um espaço depois da igualdade. O valor deverá
		 * ser apresentado com 3 casas após o ponto.
		 */

//		double volume_esfera, raio, volume, pi = 3.14159;
//		
//		raio = sc.nextDouble();
//		raio = Math.pow(raio, 3);
//		volume= (4.0/3) * pi*raio;
//		
//		System.out.printf("total %f", volume);

		

		
	}

}
